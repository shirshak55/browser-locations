# Local Examples

Run crate examples from the workspace root:

```bash
cargo run -p chrome-locations --example print_chrome_path
cargo run -p helium-locations --example print_helium_path
cargo run -p browser-locations --example print_all_browsers
```
